# Launch new webkit instance

Launch new webkit instance

## Usage

``` r
new_webkit(
  start_server = T,
  serverOptions = list(host = "127.0.0.1", port = 3000)
)
```

## Examples

``` r
if (FALSE) { # \dontrun{
browser <- rplaywright::new_webkit()
} # }
```
